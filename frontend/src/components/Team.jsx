import React from "react";
import { data } from "../restApi.json";
const Team = () => {
  return (
    <section className="team" id="team">
      <div className="container">
        <div className="heading_section">
          <h1 className="heading">OUR TEAM</h1>
          <p>
          Our team at Foodhub is a cohesive unit of passionate individuals dedicated to delivering exceptional service and culinary excellence. From our skilled chefs to our friendly servers, everyone is committed to ensuring your dining experience exceeds expectations. With a shared love for food and hospitality, we work together seamlessly to create memorable moments for our guests.
          </p>
        </div>
        <div className="team_container">
          {data[0].team.map((element) => {
            return (
              <div className="card" key={element.id}>
                <img src={element.image} alt={element.name} />
                <h3>{element.name}</h3>
                <p>{element.designation}</p>
              </div>
            );
          })}
        </div>
      </div>
    </section>
  );
};

export default Team;